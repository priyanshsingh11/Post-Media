<!doctype html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body>
        <div class="w-full h-screen bg-zinc-900 p-10">
            <div class="w-full flex justify-end">
                <a class="bg-red-500 rounded px-3 py-2 text-sm" href="/logout">Logout</a>
            </div>
            <h5 class="mb-5 text-white">you can edit your post </h5>
            <form action="/update/<%=post._id%>" method="post">
                <textarea placeholder="What's on your mind?" class="p-3 w-1/3 outline-none text-white resize-none bg-transparent border-2 border-zinc-800 rounded" name="content"><%=post.content%></textarea>
                <input class="px-3 w-40 py-2 bg-blue-500 block rounded mt-2" type="submit" value="Done">
            </form>
        
        </div>
    </body>
</html>