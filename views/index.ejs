<!doctype html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body>
        <div class="w-full h-screen bg-zinc-900 p-10">
            <h3 class="text-white text-3xl mb-6">Create Account</h3>
            <form action="/register" method="post">
                <input class="px-3 py-2 rounded outline-none bg-transparent border-2 border-zinc-800" type="text" name="name" placeholder="Enter your name">
                <input class="px-3 py-2 rounded outline-none bg-transparent border-2 border-zinc-800" type="text" name="username" placeholder="Enter your username">
                <input class="px-3 py-2 rounded outline-none bg-transparent border-2 border-zinc-800" type="number" name="age" placeholder="Enter your age">
                <input class="px-3 py-2 rounded outline-none bg-transparent border-2 border-zinc-800" type="password" name="password" placeholder="Enter your password">
                <input class="px-3 py-2 rounded outline-none bg-transparent border-2 border-zinc-800" type="email" name="email" placeholder="Enter your email">
                <input class="px-3 py-2 rounded bg-blue-500" type="submit" value="Create Account">
            </form>
        </div>
    </body>
</html>